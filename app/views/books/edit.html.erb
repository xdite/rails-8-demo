<div class="container mx-auto px-4 py-8">
  <div class="max-w-2xl mx-auto">
    <div class="bg-white rounded-lg shadow-lg p-8">
      <div class="mb-6">
        <%= link_to "← Back to Book", book_path(@book), class: "text-blue-500 hover:text-blue-600 font-medium" %>
      </div>
      
      <h1 class="text-3xl font-bold text-gray-800 mb-8">Edit Book</h1>
      
      <%= render 'form', book: @book, submit_text: 'Update Book', cancel_path: book_path(@book) %>
    </div>
  </div>
</div>
